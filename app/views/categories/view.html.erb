<style type="text/css">
	.header{
		text-align: center;
	}
	.formButtons{
		text-align: center;
	}
	.formCol{
		text-align: center;
	}
</style>
<div class="row">
	<h2 class="header"><%= @category.name %></h2>
</div>
	<% @thumb_path = "/public/videos/thumb_" %>
	<% @thumb_path += @video.vfile.url[15..-5] %>
	<% @thumb_path += ".png" %>
	<img src="<%= @thumb_path %>">